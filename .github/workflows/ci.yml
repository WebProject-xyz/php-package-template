on:
  push:
    branches:
      - "main"
  pull_request:

name: "CI"

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  ci:
    uses: "WebProject-xyz/.github/.github/workflows/ci.yml@75ee30d43ef224c273ef452fafdf5b8c09f05dac" # main
    secrets: inherit
    with:
      php-version: "8.5"
      test-matrix: |
        [
          {"php-version": "8.5", "dependencies": "highest", "with_coverage": true}
        ]
